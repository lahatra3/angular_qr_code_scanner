import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.itudiant',
  appName: 'ITudiant',
  webDir: 'dist/qr_code_scanner',
  bundledWebRuntime: false
};

export default config;
